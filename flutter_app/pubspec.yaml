name: lyam_biometric_gate
description: Lyam Face Recognition Gate Attendance System Mobile App.
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+9
  geolocator: ^10.1.0
  shared_preferences: ^2.2.2
  http: ^1.1.0
  intl: ^0.19.0
  cupertino_icons: ^1.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
